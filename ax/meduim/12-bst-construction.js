// * Average:
// - time
// Insertion O(log(N))
// Search O(log(N))
// - space
// - if recursively : O(log(N))
// - if iteratively : O(1)

//* Worst:
// Time = O(N)
// - if recursively : O(N)
// - if iteratively : O(1)
